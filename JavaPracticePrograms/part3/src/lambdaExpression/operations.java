package lambdaExpression;

interface operations {
	void result(int a,int b);
}
/*
Lambda Function -  with lambda expression we don't need to override the functions
also for one functional interface there should be exactly one abstract method, in order to use lambda expression.

*Can lambda be used with normal interfaces?
No, it should only be used with an functional interface (who has exactly one abstract method)
*/
